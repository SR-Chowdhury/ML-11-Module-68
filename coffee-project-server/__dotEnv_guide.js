/**
 * ---------------------------------------------------
 *      Express dotenv [hide username and password]
 * ---------------------------------------------------
 * 
 * step 1: import => require('dotenv').config();
 * step 2: create an .env in the root directory
 *      2.1: DB_USER = XYZ
 * step 3: const uri= `xyz/${process.env.DB_USER}........`
 * step 4: add .env in .gitignore
 */